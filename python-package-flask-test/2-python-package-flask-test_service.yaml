apiVersion: v1
kind: Service
metadata:
  name: python-package-flask-test-service
  labels:
    run: python-package-flask-test-deployment
spec:
  type: NodePort
  selector:
    run: python-package-flask-test-deployment    
  ports:
  - port: 5000
    targetPort: 5000

